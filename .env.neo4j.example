# Neo4j Authentication Configuration
# Copy this file to /opt/veritas/.env.neo4j and update with your credentials
# DO NOT commit the actual .env.neo4j file to Git!

# Neo4j credentials in combined auth format (used by docker-compose.yml)
NEO4J_AUTH=neo4j/your-secure-password-here

# Separate variables for application services
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-secure-password-here

# Backward-compatible variable names (generated by setup script)
NEO4J_AUTH_USER=neo4j
NEO4J_AUTH_PASSWORD=your-secure-password-here

# Database name (optional, defaults to neo4j)
NEO4J_DATABASE=neo4j

# Security note: Change the default password for production use
# Generate a strong password: openssl rand -base64 32
